!   wc - вывод строк, слов и символов(байт)
!   cat - может вывести содержимое всего файла на экран 
!   & - Запуск команды в фоновом режиме
!   grep - это поиск внутри текстового файла
!   /dev/null - мы скрываем вывод ошибок 
!   redirect bash:
        << метка_конца // Далее следует «встроенный документ», оканчивающийся меткой_конца
        > - файл будет перезаписан
        >> - добавление в файл
!   pipe|bash - это однонаправленный канал межпроцессного взаимодействия. 
    Конвеер обеспечивает асинхронное выполнение команд с использованием буферизации ввода/вывода.
    Таким образом все команды в конвейере работают параллельно, каждая в своем процессе.
!   chmod - устанавливает права:
        chmod u+x file - разрешить выполнения скрипта владельцу
!   date - Для получения текущих даты и времени в стандартном формате
    date +"%H:%M %d/%m/%Y" - 19:33 08/06/2017
!   shebang - в программировании последовательность из двух символов: #! в начале файла скрипта.
    #!/bin/sh - то загрузчик запускает на выполнение "/bin/sh" 
!   od -   утилита в Unix-системах для вывода дампа файла в восьмеричном формате